// user.js (Back End - Model)
// Defines a temporary in-memory array to store user data.
// Placeholder until a proper database (e.g., MongoDB or PostgreSQL) is put in.
// The array is exported for use in controllers and routes to manage user data.


const users = []; // Temporary in-memory store (replace with DB in Phase 4 or 5)
// This is a temporary in-memory array to store user data. In Phase 4,
// you’ll replace this with a proper database like MongoDB or PostgreSQL.

module.exports = users;
//This exports the users array so it can be used in other files (e.g., in your routes or controllers).